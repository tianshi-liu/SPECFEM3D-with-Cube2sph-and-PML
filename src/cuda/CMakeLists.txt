
if(ENABLE_CUDA)
    file(GLOB SOURCE CONFIGURE_DEPENDS LIST_DIRECTORIES FALSE "*.cu" )
else ()
    file(GLOB SOURCE CONFIGURE_DEPENDS LIST_DIRECTORIES FALSE "*.c" )
endif()
add_library(cuda ${SOURCE})
set_target_properties(cuda PROPERTIES CUDA_ARCHITECTURES native)